#include "holberton.h"

/**
 * mul - is multplication.
 * @a: is a variable
 * @b: is a variable
 * Description: multiplying variables
 * Return: Always 0.
 */

int mul(int a, int b)

{
	int product;

	product = (a * b);
	return (product);
}
